% Renders the stability/safety figure of Section III in several versions (one per page):
%   v0: the figure as in safe_control.tex
%   v1: revised HJ-reachability panel (d), tilted CBF set in (e), dashed maximal safe set in (e) and (f)
%   v2: as v1, but the unsafe region is shaded red and labelled in (d)-(f) with a solid gray edge, and the maximal safe set in (d) is strictly inside the constraints
% Compile all versions:  latexmk -pdf main.tex   (or pdflatex main.tex twice)
% Single-version PDFs:   pdflatex -jobname=safety_v1 "\def\variant{v1}\input{main.tex}"
%                        pdflatex -jobname=safety_v2 "\def\variant{v2}\input{main.tex}"
\IfFileExists{IEEEtran.cls}{\documentclass[conference]{IEEEtran}}{\documentclass{article}\usepackage[margin=0.75in]{geometry}}
\usepackage{amsmath,amssymb}
\usepackage{tikz}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\providecommand{\edits}[1]{#1}       % the paper marks edited text; plain here
\providecommand{\variant}{all}
\begin{document}
\makeatletter
\def\@tmp{all}\ifx\variant\@tmp
	% the page labels are set inside the figure files (guarded by \ifdefined\variantname)
	\def\variantname{Version 0}\input{fig_safety_v0.tex}
	\clearpage
	\def\variantname{Version 1}\input{fig_safety_v1.tex}
	\clearpage
	\def\variantname{Version 2}\input{fig_safety_v2.tex}
\else
	\input{fig_safety_\variant.tex}
\fi
\makeatother
\end{document}
